## [REFACTOR] CSS Rebuild and Markup Conversion Alignment for Shop Application

Date: 2026-06-26 12:00 UTC
Actor: agent:antigravity

### Context

The shop application required the completion of CSS recreation and styling alignment matching the mockups in `e:\hsc\Mockups\shop_basic`.

### Changes

1. **Rebuilt `globals.css` (`apps/shop/src/app/globals.css`):**
   - Added Tailwind CSS v4 theme configuration with appropriate `--text-*` variables for typography sizes (`body-md`, `body-lg`, `headline-md`, `headline-lg`, `display-lg`, `label-sm`, `label-md`).
   - Defined base custom components like `.cart-item-hover` and `.bento-card` used by the page mockups.
2. **Updated next.config.ts (`apps/shop/next.config.ts`):**
   - Configured `remotePatterns` to allow loading product images from `lh3.googleusercontent.com`.
3. **Fixed Checkout SVG Attributes (`apps/shop/src/app/checkout/page.tsx`):**
   - Replaced invalid JSX attribute `stroke-width` with React-compatible camelCase `strokeWidth` on the SVG element.
4. **Fixed Footer Typography Classes (`apps/shop/src/components/Footer.tsx`):**
   - Replaced non-existent `text-headline-sm` and `font-headline-sm` with design system compliant classes `text-headline-md` and `font-headline-md`.

### Reasoning

* **What problem would remain unsolved if this change were not made?**
  - Text styles and custom layout components (such as Bento cards and hover states in cart/profile) would fail to render according to the approved designs.
  - Product images from external domains would fail to load due to Next.js image optimization policies.
  - The build process would trigger JSX/TS compilation errors due to the invalid SVG attribute naming convention.

### Impact

* **Production behavior**: None (rebuilt styles match mockup expectations).
* **API contracts**: None.
* **Database schema**: None.
* **User experience**: The CSS styles now render correctly, matching design mockups precisely.
* **Infrastructure**: None.

### Risks

None. All styles map exactly to mockups and the production build compiles successfully.

### Verification

Ran `pnpm --filter shop build` which completed successfully:
- Compiles TS and Next.js successfully without errors.
- Generates static pages.

### Rollback

Revert the modified files to their previous git commits:
- `git checkout -- apps/shop/src/app/globals.css apps/shop/next.config.ts apps/shop/src/app/checkout/page.tsx apps/shop/src/components/Footer.tsx`

### References

- Workspace Conversion Workflow: `e:\hsc\hscshop\.agents\workflows\ui-conversion.md`
- Mockups: `e:\hsc\Mockups\shop_basic`
