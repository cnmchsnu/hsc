## [FEAT] UI/UX Conversion for Campus Event Application

Date: 2026-08-05 11:34 UTC
Actor: agent:antigravity

### Context

Convert provided mockups in `mockups/campus-event` into production-ready Next.js pages inside `apps/campus-event` adhering to `docs/ui-conversion-rules.md`, `docs/page-overview.md`, and design themes (`neon_pulse`, `campus_yellow`, `campaign_engine_studio_v2`).

### Changes

1. **Configured Styling Import & Design Tokens (`apps/campus-event/app/globals.css`):**
   - Implemented `@import "tailwindcss";`, `@import "@repo/ui/styles/globals.css";`, and `@source "../../**/*.tsx";`.
   - Defined Material Design 3 inspired CSS custom properties in `:root` and theme overrides for `neon_pulse` and `campus_yellow`.

2. **Created Mock Data (`apps/campus-event/src/mock/eventMockData.ts`):**
   - Defined TypeScript interfaces and mock datasets for events, tickets, merchandise, and checkout orders.

3. **Created Navigation Components (`apps/campus-event/src/components/Header.tsx`, `Footer.tsx`):**
   - Header with category links, live neon pulse indicator, cart action, and responsive navigation.
   - Footer with platform metadata and quick links.

4. **Converted App Router Pages (`apps/campus-event/app`):**
   - `app/page.tsx` — Landing page (`Linding Page` mockups, `campaign_engine_studio_v2` theme, 25vh hero, search & category pills).
   - `app/events/page.tsx` — Event catalog (`活動主頁`, notice banner, merchandise & pass grid).
   - `app/events/[id]/page.tsx` — Esports Event detail (`商品主頁-活動-電競霓虹主題`, `neon_pulse` theme, cyan/magenta glow, glass cards).
   - `app/tickets/[id]/page.tsx` — Festival Pass detail (`商品主頁-票券-黃色主題`, `campus_yellow` brutalist theme, 2px borders, hard offset shadows).
   - `app/products/[id]/page.tsx` — Merchandise Product detail (`商品主頁-商品-棕色主題`, size/color selector, quantity control).
   - `app/events/[id]/register/page.tsx` — Registration form (`活動報名表單`, input fields, department picker).
   - `app/checkout/page.tsx` — Payment page (`付款`, LINE Pay / Credit / ATM selector, simulated payment success confirmation).

5. **Updated Documentation (`docs/page-overview.md`):**
   - Updated route status for `/events` and `/events/[id]` to `[x]`.
   - Appended conversion log entries under `# UI Conversion Tracking`.

### Reasoning

* **What problem would remain unsolved if this change were not made?**
  - The campus-event application would remain an empty starter app without responsive pages or design theme support.
  - Stakeholders would not be able to preview or test the user flows for events, tickets, and merchandise ordering.

### Impact

* **Production behavior**: Complete UI/UX implementation of campus-event app.
* **API contracts**: None (pure UI conversion with mock data).
* **Database schema**: None.
* **User experience**: Fluid navigation across 7 key screens with full visual fidelity to design mockups and themes.

### Risks

None. All routes consume mock data with zero backend dependencies.

### Verification

Executed TypeScript build check for the campus-event package:
`pnpm --filter campus-event run build`

### References

- Guidelines: `docs/ui-conversion-rules.md`
- Page overview: `docs/page-overview.md`
- Design systems: `mockups/campus-event/neon_pulse`, `campus_yellow`, `campaign_engine_studio_v2`
