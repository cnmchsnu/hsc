name: Mirror Production Repository

on:
  push:
    branches:
      - main


jobs:
  mirror:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push Mirror
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote add mirror https://x-access-token:${{ secrets.HSCSHOP_MIRROR_DEPLOY_PROD }}@github.com/hsnu-cnme/hsc-prod-deploy.git
          git push mirror HEAD:main --force